import 'package:flutter/material.dart';

class Pol extends StatelessWidget {
  const Pol({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
        AlertDialog(
        title: Text("ooy",style: TextStyle(
              fontWeight: FontWeight.bold
            )),
      )
        ],
      ),
    );
    }}